name: market_signal_pipeline
version: "1.0.0"
config-version: 2

profile: market_signal_pipeline

model-paths: [ "models" ]
test-paths: [ "tests" ]
macro-paths: [ "macros" ]
snapshot-paths: [ "snapshots" ]
seed-paths: [ "seeds" ]

clean-targets:
  - "targets"
  - "dbt_packages"

models:
  market_signal_pipeline:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
